//- TODO:========== 每次記得修改標題、切換單雙月 ==========
- var mainTitleAlt = "母親最大牌";
//- 打開雙月選項切換雙月(預設單月)
//- - var isEven = true

//- 根據需要選擇要include的頁面並build就可以產出e、u、主頁面、sp頁四包
doctype html
html(lang='zh-Hant-TW')
  head
    include ./include/_meta.pug
  body
    //- 每頁上方都要放動態產出置頂元件的定位tag(e.g. 電梯、浮水印)
    include ./include/_top-home-section.pug

    //- main section 主視覺區
    //- TODO:========== 此處為手動放置要產出的頁面，記得style.sass也要修改 ==========
    //- include ./pages/_index-main-e.pug
    include ./pages/_index-main-u.pug
    //- include ./pages/_index-sp-e.pug
    //- include ./pages/_index-sp-u.pug

    //- bundler script entry point
    script(src="../js/bundle.js")

    //- TODO:========== dist後注意事項 ==========
    //- 1. index_fb.jpg會被parcel加上hash tag，記得出去拔掉
    //- 2. 要拿給設計加工的把壓縮程式碼存回巢狀程式碼
